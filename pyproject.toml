[project]
name = "kimi-k2-thinking-k8s"
version = "0.1.0"
description = ""
readme = "README.md"
authors = [{ name = "Romain Graux", email = "author@romaingrx.com" }]
requires-python = ">=3.13"
license = "MIT"
license-files = ["LICENSE"]

dependencies = []

[project.optional-dependencies]
dev = ["basedpyright", "ruff", "pytest", "pre-commit"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/kimi_k2_thinking_k8s"]

[tool.basedpyright]
reportAny = false
reportExplicitAny = false
reportUnusedCallResult = false

[tool.isort]
profile = "black"

[tool.pytest.ini_options]
testpaths = ["tests"]
norecursedirs = [] # If want to ignore some dirs
addopts = [
    # Use modern Python imports (i.e., don't require __init__.py files everywhere)
    "--import-mode=importlib",
    # Require markers to be explicitly defined
    "--strict-markers",
]
